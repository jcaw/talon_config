# Emacs-related commands, that are available outside Emacs.
-
talon search <user.dictation>: user.emacs_search_talon_user(dictation)
talon open <user.dictation>:   user.emacs_find_file_talon_user(dictation)

(add | new) word: user.emacs_open_custom_words()
